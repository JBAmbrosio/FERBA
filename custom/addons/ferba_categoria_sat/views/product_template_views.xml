<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- La pestana de contabilidad del producto (cuentas y clave SAT) se oculta
         en la empresa que contabiliza por categoria, salvo para el gestor de
         categorias. -->
    <record id="product_template_form_contabilidad_por_categoria" model="ir.ui.view">
        <field name="name">product.template.form.contabilidad.por.categoria</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="priority">100</field>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='invoicing']" position="attributes">
                <attribute name="invisible">not ferba_ve_contabilidad</attribute>
            </xpath>
            <xpath expr="//field[@name='categ_id']" position="after">
                <field name="ferba_ve_contabilidad" invisible="1"/>
            </xpath>
        </field>
    </record>

    <record id="product_normal_form_contabilidad_por_categoria" model="ir.ui.view">
        <field name="name">product.product.form.contabilidad.por.categoria</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_normal_form_view"/>
        <field name="priority">100</field>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='invoicing']" position="attributes">
                <attribute name="invisible">not ferba_ve_contabilidad</attribute>
            </xpath>
            <xpath expr="//field[@name='categ_id']" position="after">
                <field name="ferba_ve_contabilidad" invisible="1"/>
            </xpath>
        </field>
    </record>
</odoo>
